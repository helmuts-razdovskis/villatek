import { Button } from 'primereact/button';

function MenuButton(props) {
    return (
        <Button icon="pi pi-bars" className="p-button-rounded p-button-text" />
    );
}

export default MenuButton;